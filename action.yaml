name: 'Read TOML'
description: 'Reads a specified key from a TOML file'
inputs:
  path:  
    description: 'Path to TOML file, relative to /github/workspace/'
    required: true
  key: 
    description: 'Absolute key to value in TOML file'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - --path 
    - /github/workspace/${{ inputs.path }}
    - --key 
    - ${{ inputs.key }}
